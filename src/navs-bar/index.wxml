<view class="panel">
  <navigator wx:for="{{navs}}" url="{{item.to}}" wx:key="text" class="nav">
    <image src="{{item.icon}}" class="icon"></image>
    <view class="label">{{item.label}}</view>
    <text class="info">{{item.text}}</text>
    <view class="arrow"></view>
  </navigator>
</view>